{
  "name": "@gitpkg/core",
  "version": "1.0.0-alpha",
  "main": "dist/index.js",
  "types": "dist/index.d.js",
  "license": "MIT",
  "scripts": {
    "test": "jest",
    "build": "rimraf dist && tsc -p tsconfig.prod.json --outDir dist --declaration --sourceMap --declarationMap"
  },
  "dependencies": {
    "@types/tar-stream": "^3.1.3",
    "got": "^10.5.5",
    "path-to-regexp": "^6.1.0",
    "tar-stream": "^3.1.6"
  }
}
